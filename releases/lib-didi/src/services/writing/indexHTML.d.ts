import { ILibDidiContext } from '../../types/machine.types';
export declare const indexHTML: ({ options }: {
    options: any;
}, { importMap, constants }: Partial<ILibDidiContext>) => Promise<void>;
